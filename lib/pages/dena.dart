import 'package:flutter/material.dart';

class Dena extends StatelessWidget {
  const Dena({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dena'),
    );
  }
}
