import 'package:flutter/material.dart';

class Lena extends StatelessWidget {
  const Lena({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Lena'),
    );
  }
}
